import React from 'react';
import ProductsManager from './components/ProductsManager';

const page = () => {
  return (
    <div>
      <ProductsManager/>
    </div>
  );
};

export default page;